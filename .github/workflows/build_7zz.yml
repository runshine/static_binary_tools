name: build_7zz
on:
  push:
    branches:
      - '**'
    paths:
      - .github/workflows/build_7zz.yml
      - build/7zz/*

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push node_vpn
        uses: docker/build-push-action@v5
        with:
          context: build/7zz/
          platforms: linux/amd64,linux/arm64/v8,linux/arm/v7,linux/arm/v5,linux/riscv64
          push: false
          tags: local_7zz:latest
          file: Dockerfile
          outputs: |
            type=local,dest=${{ github.workspace }}/output

      - name: Upload binary to artifacts
        uses: actions/upload-artifact@v4
        with:
          path: ${{ github.workspace }}/output/build/install/bin/*

